# Flight_Price_Prediction
A machine learning project that predicts airline ticket prices using Linear Regression and Random Forest Regression. The project includes complete preprocessing, feature engineering, model training, evaluation (R², MAE, RMSE), and final prediction generation. The Random Forest model is selected as the best-performing model and saved for deployment.
